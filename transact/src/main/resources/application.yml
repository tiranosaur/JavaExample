spring:
  application:
    name: transact

  datasource:
    url: jdbc:mysql://localhost:3306/png
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: tiger

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect: MySQLDialect